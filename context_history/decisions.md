# Decisions Log — OpenClippy

| Date | Decision | Reasoning | Alternatives Considered |
|------|----------|-----------|------------------------|
| 2026-03-10 | Treat Teams tools being filtered out in read-only profile as documented behavior, not a bug | The `filterToolsByProfile` uses suffix-based pattern matching consistently; Teams tools use non-standard suffixes (`_chats`, `_chat`, `_channels`, `_messages`) that don't match allowed patterns (`_list`, `_read`, etc.) | Fix naming convention to match (breaking change), add Teams-specific patterns to read-only rules (ad-hoc) |
| 2026-03-10 | Use real service modules in service-registration tests, mock only external APIs in agent-flow tests | Service-registration tests verify wiring correctness; agent-flow tests need controlled API responses | Mock everything (loses integration value), use real APIs (flaky, slow, requires credentials) |
| 2026-03-10 | Fix WS test race condition by registering message listener before connection opens | Server sends "connected" immediately; sequential connect→listen misses the message between steps | Delay server message (fragile), use event buffering (over-engineered) |
| 2026-03-10 | Isolate WS tests with per-test gateway instances, share gateway for HTTP tests | WS tests need clean session state; HTTP tests are stateless and benefit from shared setup | Single shared gateway for all (cross-test interference), per-test for all (slow HTTP tests) |
| 2026-03-10 | Defer Teams Bot (Task 2.3) to avoid botbuilder SDK dependency bloat | botbuilder adds significant dependency tree; other Phase 2 tasks deliver more value first | Install botbuilder now (large dep), skip Teams entirely (loses key feature) |
| 2026-03-10 | Use PID file for gateway daemon management instead of process signaling | Simple, portable, works across CLI invocations; standard Unix daemon pattern | D-Bus/IPC (overkill), lock file (less info), systemd socket (not portable) |
| 2026-03-10 | OneNote read bypasses graphRequest, uses direct fetch | Graph returns HTML for page content, not JSON; graphRequest parses JSON | Custom Content-Type header on graphRequest (awkward), separate HTML client (over-engineered) |
| 2026-03-10 | Planner update requires If-Match etag for optimistic concurrency | Graph API enforces this for Planner resources; prevents conflicting concurrent edits | Skip etag (API would reject), lock-based approach (not supported by Graph) |
| 2026-03-10 | Graceful degradation catches 401/403/429/network but passes through 404 | 404 is a valid application error (resource not found), not a service degradation | Catch all errors (masks real bugs), catch nothing (poor UX on transient failures) |
| 2026-03-10 | Memory store uses SQLite WAL journal mode | Allows concurrent reads during writes, better performance for agent use case | Default journal mode (blocks reads during writes), in-memory only (no persistence) |
| 2026-03-10 | GitHub repo under `revsmoke` (personal), public visibility | `revsmoke` is the authenticated GitHub user; `twoedge` was macOS username used as placeholder in package.json | Create under an org (none fit), private repo (open-source intent) |
| 2026-03-10 | Use recursive question() instead of for-await in select() | `for await (const line of rl)` permanently closes the readline.Interface on loop exit, breaking subsequent prompts. Recursive `.then()` chaining preserves the stream. | for-await with re-create (wasteful), event listeners (more complex), inquirer.js (new dependency) |
| 2026-03-10 | Wizard writes toolProfile only to agent.toolProfile, not tools.profile | Single source of truth avoids drift; agent.toolProfile is the canonical location used by tool system | Write to both (redundant, confusing), write only to tools.profile (inconsistent with agent config) |
| 2026-03-10 | Wizard outputs minimal config (only non-default values) | Clean config files, future default changes propagate automatically, easier to read | Write all values (verbose, blocks default changes), write only user-modified (harder to implement) |
| 2026-03-10 | ServiceId → string (not branded type) | Simplest change for plugin extensibility; plugins need arbitrary IDs that can't be enumerated at compile time | Keep union + module augmentation (complex TS), branded string type (extra boilerplate) |
| 2026-03-10 | Runtime shape validation for dynamically loaded plugins | TypeScript types can't be trusted for external code loaded via import(); shape checking prevents crashes from malformed modules | Trust exports (unsafe), require TypeScript source (limits plugin authors) |
| 2026-03-10 | Defense-in-depth path traversal prevention | Manifest-level validation (rejecting .., absolute paths, non-.js extensions) catches obvious attacks; loader-level resolve()+startsWith() is safety net | Manifest-only (single point of failure), loader-only (late detection) |
| 2026-03-10 | PluginRegistry never-throw pattern | One bad plugin shouldn't crash startup; errors collected and returned as PluginError[] for reporting | Fail-fast (one plugin kills the app), retry (complex, unlikely to help) |
| 2026-03-10 | PluginsConfig type in types.ts not registry.ts | Config types belong in type definition files, not runtime modules; follows existing codebase convention | Keep in registry.ts (co-location with usage), separate config types file (over-split) |
| 2026-03-10 | Discover plugins BEFORE creating readline in wizard | Plugin discovery is async I/O (filesystem scan); doing it after readline creation causes async gaps that drain mock input streams in tests, breaking test reliability | Discover inside readline session (test-hostile), lazy discover on demand (complex state) |
| 2026-03-10 | Accept pluginOptions as optional param for wizard testability | Allows tests to inject known PromptOption[] directly, bypassing async PluginRegistry.loadAll() for deterministic testing | Always call loadAll() (untestable without complex mocks), global test override (fragile) |
| 2026-03-10 | Filter colliding plugin serviceIds silently instead of throwing | Graceful UX: if a plugin shadows a builtin service ID, the builtin wins; user doesn't see confusing errors for edge cases | Throw error (bad UX), show warning (clutters wizard), let plugin win (breaks builtins) |
| 2026-03-11 | Centralize helper functions in shared tool-utils.ts rather than per-service locals | 9 functions were duplicated across 10+ files with minor format variations; central module eliminates drift and ensures consistent error messages | Keep per-service helpers (duplication), use a class (over-engineered for simple pure functions) |
| 2026-03-11 | Standardize error message formats during deduplication | onenote used "Error: X is required" vs others using "Missing required parameter: X"; teams-chat omitted "Error: " prefix. Standardized to consistent formats. | Preserve original formats per service (confusing inconsistency), add format parameter (over-engineered) |
| 2026-03-11 | Place BUILTIN_SERVICE_IDS in types.services.ts, not builtin-modules.ts | Avoids circular imports: builtin-modules imports service modules which import from types.services; if types.services imported back from builtin-modules that's circular | Put in builtin-modules.ts (circular dep risk), separate constants file (unnecessary indirection) |
| 2026-03-11 | Use DEFAULT_CONFIG for Azure credential fallbacks instead of hardcoded strings | Single source of truth — Azure client/tenant IDs were hardcoded in 3 files; now config/defaults.ts is the only place they appear | Keep separate hardcoded values (drift risk), env-only fallback (breaks zero-config experience) |
| 2026-03-11 | Skip graphClientMockFactory() rollout to vi.mock() blocks | vitest hoists vi.mock() calls above imports, so factory functions defined elsewhere can't be called inside vi.mock(); focused on createToolContext() rollout instead | Inline factory in each test (defeats purpose), restructure all test files (too invasive) |
