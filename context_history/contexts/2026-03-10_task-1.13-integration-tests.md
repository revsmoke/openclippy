## Task 1.13: Integration Tests — 2026-03-10 11:40

### Phase
testing

### Summary
Created two integration test files for the OpenClippy project: `service-registration.test.ts` (12 tests) verifying all 4 priority service modules register correctly and expose expected tools with proper profile filtering, and `agent-flow.test.ts` (11 tests) testing the full agent loop end-to-end with mocked Anthropic SDK and Graph API. All 266 tests pass (23 new + 243 existing).

### Files Created/Modified
| File | Purpose |
|------|---------|
| `/Users/twoedge/Dev/openclippy/tests/integration/service-registration.test.ts` | 12 tests: service registration, tool collection (31 total), profile filtering (read-only/standard/full), system prompt building, config validation, metadata checks, no duplicate tool names |
| `/Users/twoedge/Dev/openclippy/tests/integration/agent-flow.test.ts` | 11 tests: full agent loop with mocked Anthropic API, Graph API mocking (graphRequest + graphPaginate), tool execution, error handling, multi-turn conversations, read-only profile enforcement, maxTurns safety limit |

### Key Decisions
- Used real service modules (not mocks) in service-registration tests: Tests the actual wiring, not mock behavior
- Mocked `@anthropic-ai/sdk` and `../../src/graph/client.js` in agent-flow tests: Isolates from external APIs while testing real tool execution logic
- Documented Teams tool naming gap in read-only profile as a concern, not a bug fix: The behavior is consistent with the current `filterToolsByProfile` implementation; changing it would be a design decision

### Technical Details
- Tool count: Mail=11, Calendar=8, ToDo=6, Teams=6, Total=31
- Teams tools use non-standard suffixes (e.g., `teams_list_chats` instead of `teams_list`) which causes all Teams tools to be filtered out in read-only mode
- `calendar_list` uses `graphPaginate` (returns flat array), unlike other tools which use `graphRequest` (returns `{value: [...]}`)
- `teams_send` expects `content` parameter, not `message`
- Vitest mock hoisting requires `vi.mock()` calls before imports

### Testing/Verification
```
pnpm test — 266 tests passed across 15 test files (657ms)
```
No regressions introduced. All existing 243 tests continue to pass.

### State
completed

### Next Steps
- Consider adding Teams-specific suffixes to read-only allowed patterns (or renaming Teams tools to follow standard convention)
- No commit was made per user instruction — commit when ready

### Related Files
- `/Users/twoedge/Dev/openclippy/src/agents/tool-profiles.ts` — Profile filtering logic with suffix patterns
- `/Users/twoedge/Dev/openclippy/src/services/registry.ts` — ServiceRegistry class
- `/Users/twoedge/Dev/openclippy/src/agents/runtime.ts` — Agent loop implementation
