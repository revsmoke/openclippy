# Build & dependencies
node_modules/
dist/
coverage/
# repo uses pnpm — pnpm-lock.yaml is the canonical lockfile
package-lock.json

# Secrets & local state
.env
src/auth/*.json
*.log
.claude/launch.json

# OS / editor noise
.DS_Store
*.psd

# Local-only project memory & planning artifacts
PLAN.md
CURRENT_PLAN.md
agent_plans/
context_history/

# Local scratch files (not part of the shipped repo)
test-ws.mjs
images/openclippy_sprites.svg
images/openclippy.html
images/openclippy-gsap.html
